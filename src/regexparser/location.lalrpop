use crate::regexparser::ast::*;

grammar;

pub Location: Box<Location> = {
    "%" => Box::new(Location::All),
    <x:r"<"> <s:r"[^%:<>()]*"> <y:r">:"> => Box::new(Location::Path(s.to_string())),
    "(" <s:r"[^%:<>()]*"> "):" => Box::new(Location::Function(s.to_string())),
};